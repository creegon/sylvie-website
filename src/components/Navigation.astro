---
// 导航栏组件 - 带猫耳装饰
const navItems = [
  { href: "/", label: "首页", emoji: "🏠" },
  { href: "/about", label: "关于我", emoji: "🐱" },
  { href: "/blog", label: "日记", emoji: "📝" },
  { href: "/books", label: "书架", emoji: "📚" },
  { href: "/gallery", label: "画廊", emoji: "🎨" },
];

const currentPath = Astro.url.pathname;
---

<nav class="sticky top-0 z-50 backdrop-blur-md border-b" 
     style="background-color: rgba(250, 248, 245, 0.9); border-color: var(--color-primary-light);">
  <div class="max-w-5xl mx-auto px-4">
    <div class="flex items-center justify-between h-16">
      <!-- Logo / 名字 -->
      <a href="/" class="flex items-center gap-2 group">
        <!-- 猫耳装饰 -->
        <span class="text-2xl relative">
          <span class="inline-block group-hover:animate-[earWiggle_0.5s_ease-in-out]">🐱</span>
        </span>
        <span class="text-lg font-semibold" style="font-family: var(--font-display); color: var(--color-secondary);">
          苏尔薇
        </span>
      </a>
      
      <!-- 导航链接 -->
      <ul class="flex items-center gap-1 sm:gap-4">
        {navItems.map((item) => {
          const isActive = currentPath === item.href || 
            (item.href !== "/" && currentPath.startsWith(item.href));
          return (
            <li>
              <a 
                href={item.href}
                class:list={[
                  "px-3 py-2 rounded-lg text-sm transition-all duration-300",
                  "hover:scale-105 flex items-center gap-1",
                  isActive 
                    ? "font-semibold shadow-sm" 
                    : "hover:bg-white/50"
                ]}
                style={isActive ? "background-color: var(--color-bg-card); color: var(--color-accent);" : "color: var(--color-text);"}
              >
                <span class="hidden sm:inline">{item.emoji}</span>
                <span>{item.label}</span>
              </a>
            </li>
          );
        })}
      </ul>
    </div>
  </div>
</nav>

<style>
  @keyframes earWiggle {
    0%, 100% { transform: rotate(0deg); }
    25% { transform: rotate(-10deg); }
    75% { transform: rotate(10deg); }
  }
</style>
